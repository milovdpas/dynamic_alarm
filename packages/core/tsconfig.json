{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "module": "node18",
    "rootDir": "src",
    "outDir": "dist"
  },
  "$comment": "@alarm/types resolves via the npm-workspaces symlink to its dist/ typings; the project reference below guarantees it is built first.",
  "references": [{ "path": "../types" }],
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules", "**/*.test.ts"]
}
